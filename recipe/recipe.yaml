schema_version: 1

context:
  version: "0.105"

package:
  name: holidays
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/h/holidays/holidays-${{ version }}.tar.gz
  sha256: fc9abc0c187b62e955f92aa12dbe7ed1998cc94712f295ec9244db788594d662

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-build-isolation --no-deps

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - python-dateutil >=2.9.0.post0,<3
tests:
  - python:
      imports:
        - holidays
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: MIT
  license_file: LICENSE
  summary: Generate and work with holidays in Python
  description: |
    A fast, efficient Python library for generating country, province and state
    specific sets of holidays on the fly. It aims to make determining whether a
    specific date is a holiday as fast and flexible as possible.
  homepage: https://github.com/vacanza/python-holidays/
  repository: https://github.com/vacanza/python-holidays/
  documentation: https://python-holidays.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - bletham
    - rpanai
    - synapticarbors
